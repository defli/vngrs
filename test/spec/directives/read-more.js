'use strict';

describe('Directive: readMore', function () {

  // load the directive's module
  beforeEach(module('bookaApp'));

  var  scope;

  beforeEach(inject(function ($rootScope) {
  	scope = $rootScope.$new();
  }));

});
